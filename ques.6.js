// Given an array of strings, group anagrams together.


function Anagrams(arr){
    for(let i=0;i<arr.length;i++){
        for(let j=i+1;j<arr.length;j++){
            if(arr[i])
        }
    }
}

const arr = ['eat','tea','tan','ate','nat','bat'];
const ans = Anagrams(arr)
console.log(ans)
